function [averagePathLength, diameter] = CalcAveragePathLength(A)

n = size(A,1); 

for i = 1:n
    node = A(i,:); 
    connections = 0; 
    steps = 1; 
    pathLengthI = 0; 
    while (connections < n-1)
       newConnections = A(find(node),:); 
       newConnections = sum(newConnections);
       newConnections = 
        